from django.urls import path, include
from Authentication.views import (AddStudent, AddFaculty, AddAdmin,
AddUserRoles, Login, AddPosition, AddProgram, GetUserRole, GetAllStudents)

urlpatterns = [
       
]